sequential:
  coverage: {streams: 50, max_events: 200, checkpoint_every: 100}
  utility: {streams: 20, max_events: 200, checkpoint_every: 100}
statistics: {bootstrap_resamples: 200, sign_flip_randomizations: 200}
benchmark: {warmup_repetitions: 1, measured_repetitions: 3}
