# Progress

1. `DISCOVERED` — 2026-09-12: scanned the full production tree before edits; 226 markers in 17 modules.
2. `ANALYZED` — reviewed `types.py` and all existing enum declarations. Dynamic IDs, digests, file names, labels, error text, YAML primitives, and rendered SVG/TeX text are not closed domains and must remain distinct semantic aliases/boundaries rather than be forced into enums.
3. `IN_PROGRESS` — moving the HITL-IoT pinned contract from data code to validated configuration, and replacing genuine repeated closed labels with existing enums.
4. `IMPLEMENTED` — the HITL-IoT DOI, filenames, checksum, cohort, input/schema columns, identity columns, and named ingest fields are now validated configuration. Ingest uses that contract directly; removed `int`/`bool`/`float` coercions were replaced by the validated model boundary. Focused config, trajectory, and planning tests pass (46 tests).
5. `CORRECTIVE PAUSE` — marker deletion is suspended. The unproven marker removals in `paths.py` and `config.py` were restored after user feedback. Remaining entries will be changed only with an accompanying implementation and usage review.
6. `REVIEWED` — `paths.py`: `ExperimentSlug`, `CoordinateName`, and `CoordinateToken` now reside in `types.py`; every affected source and test consumer was migrated. Closed workspace components and checkpoint/cell-path grammar are enums. `semantic_slug` remains a deliberately generic text-processing boundary, covered by the source-audit boundary rule. Focused path/schema/layout tests: 65 passed.
7. `VERIFIED` — report export now uses `DependencyAuthority.UV_LOCK` and `RESULTS_ROOT` rather than raw duplicated tokens. Export, schema, and reproduction callers/tests: 48 passed.
8. `VERIFIED` — failure records now carry a `FailureDiagnostic` model (`ExceptionClassName`, `FailureMessage`, `TracebackText`) instead of three unrelated primitive aliases. Runner, status, persistence, and recovery callers were migrated. Focused tests: 34 passed.
9. `VERIFIED` — telemetry's closed logger name and no-current-cell label are enums. Dynamic phases and labels remain domain aliases because they encode runtime batch/cell identities. Telemetry tests: 3 passed.
10. `VERIFIED` — table output suffixes derive from `PublicationExtension`; TeX escaping remains a deliberate arbitrary-text rendering boundary. Unit and integration table/reporting tests: 15 passed.
11. `VERIFIED` — publication rows now distinguish compatibility from safety regimes using their existing enums, rather than flattening them into a string alias. Generated theorem/method display names moved to `types.py`. Source-data and synthesis tests: 64 passed.
12. `VERIFIED` — anytime minimum-information completion now uses its explicit `LawName` contract; hand-case epochs reuse central coordinate grammar. Moving coordinate grammar to `types.py` removed an import-layer cycle. Anytime/plan/provenance tests: 57 passed.
13. `VERIFIED` — endpoint and terminal partition labels are now `PartitionLabel` enum members. Constants, partitions, and comparator tests: 53 passed. The remaining constants are being classified by invariant versus configuration ownership before migration.
14. `VERIFIED` — production, smoke, and test configuration filenames are `ConfigFile` members, with `Path` retained only at the filesystem interface. Constants/config/CLI tests: 92 passed.
15. `VERIFIED` — deterministic seed prefix and field separator are `SeedMaterialGrammar` members. Constants and deterministic namespace/seed tests: 22 passed.
16. `VERIFIED` — information roundoff ULP tolerance is now validated numerical configuration and is consumed through `active_config` by the information guard. Information/config tests: 32 passed.
17. `VERIFIED` — resolved-harm safety margin is now validated numerical configuration and is consumed by safety-budget case construction. Safety/config tests: 32 passed.
18. `VERIFIED` — projection root scan, incumbent bisection, and both stall detectors now read one validated `NumericsConfig.arb_search` policy. Projection/config tests: 36 passed.
19. `REVIEWED` — fresh source search confirms all migrated numerical globals are absent. Remaining markers are confined to config/YAML boundaries, dynamic provenance/storage/type aliases, and presentation boundaries; no unrelated marker-bearing production module remains.
20. `VERIFIED` — canonical JSON object serialization uses central `JsonObject` rather than a raw boundary mapping. Storage/runner tests: 23 passed.
21. `VERIFIED` — YAML scalar/container types remain confined to configuration ingestion and are immediately coerced then Pydantic-validated. Config ownership and config tests: 33 passed.
22. `VERIFIED` — endpoint band count is validated in `MethodConfig` and all endpoint construction/naming consumers read the active configuration. Focused config, partition, comparator, sensitivity, and source-data tests: 124 passed.
23. `VERIFIED` — persistence and provenance identities now have one owner in `types.py`; closed provenance path-coordinate names use `CoordinateName`, while generated display/digest values remain semantic aliases. Focused provenance/storage/plan/schema tests: 96 passed.
24. `VERIFIED` — dynamic figure titles use `FigureTitle`; closed evidence-family diagnostics use `EvidenceFamilyLabel`; external dataset contract strings remain explicit typed configuration values rather than false enums. Reporting and architecture unfinished-marker tests: 74 passed.
25. `REVIEWED` — fresh repository scan finds no unfinished-work marker outside the architecture test that detects such markers. The touched conversion sites are serialization/parsing, plotting, or numerical-library boundaries; `git diff --check` is clean.
26. `VERIFIED` — all 855 tests passed in terminal, bounded runs: 745 unit, 58 architecture, 48 integration, and 4 end-to-end tests. The split preserves complete test coverage while respecting the environment's 30-second command window.
27. `VERIFIED` — final source audit found no unfinished markers outside the test detector and no `NewType` definitions outside `types.py`. Removed the remaining unnecessary `StrEnum.value` unwrapping; remaining primitive conversions are inspected external serialization/parsing, NumPy/scientific-library, or plotting boundaries.
